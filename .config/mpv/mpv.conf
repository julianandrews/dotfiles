# WORKAROUND: Washed-out colors in fullscreen on Hyprland (mpv + Hyprland bug)
# See: https://github.com/hyprwm/Hyprland/discussions/12829
# Can be removed when either:
#   - Hyprland implements required color management protocol events, or
#   - mpv changes its default behavior (test with newer versions)
target-colorspace-hint=no
